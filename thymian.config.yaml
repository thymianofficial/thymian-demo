# Thymian Configuration
# Generated by `thymian generate config`
#
# Specification: openapi:openapi.yaml
#
# Each `ruleSets` entry may be a bare package name (which uses that
# package's `recommended` profile — the curated, noise-tuned defaults)
# or an object `{ name, profile }` to opt into `strict` (spec-faithful
# shipped defaults) or `minimal`.
#
# For documentation, see: https://thymian.dev/references/configuration/
#
# Run `thymian lint` to validate your API specification.
# Run `thymian test` to test a live API against the specification.
#
specifications:
  - type: openapi
    location: openapi.yaml
traffic: []
ruleSets:
  - "@thymian/rules-rfc-9110"
  - "@thymian/rules-api-description-validation"
ruleSeverity: hint
rules: {}
plugins:
  "@thymian/plugin-http-linter": {}
  "@thymian/plugin-openapi": {}
  "@thymian/plugin-request-dispatcher": {}
  "@thymian/plugin-sampler": {}
  "@thymian/plugin-reporter":
    options:
      formatters: {}
  "@thymian/plugin-http-tester": {}
  "@thymian/plugin-http-analyzer": {}
  "@thymian/plugin-har": {}
  "@thymian/plugin-spectral": {}
